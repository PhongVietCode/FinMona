import React from "react";

import styles from "./ScreenHeaderButton.style";
import { Pressable } from "react-native";

export interface ScreenHeaderProps {
    icon: any,
    handlePress: any,
}

export const ScreenHeaderButton = (props: ScreenHeaderProps) =>{
    return (
       <Pressable>
        
       </Pressable>
    )
}